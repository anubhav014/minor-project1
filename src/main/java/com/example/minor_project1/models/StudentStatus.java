package com.example.minor_project1.models;

public enum StudentStatus {

    VERIFIED,

    UNVERIFIED,

    INACTIVE ///We don't keep active as it overlaps with Verified and verified makes more sense.
}
